# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# Use 'localhost' if running the database on the same machine as the server.
# If using Docker, this might be the name of the database service (e.g., 'postgres').
DB_HOST=localhost
DB_PORT=5432
DB_NAME=almadina_pos
DB_USER=postgres
DB_PASSWORD=your_super_secret_password

# JWT Configuration
# IMPORTANT: Use a long, random, and secret string for production.
JWT_SECRET=your_super_secret_jwt_key_here_please_change_me
JWT_EXPIRE=7d

# Business Logic Configuration
TAX_RATE=0.16 # Example: 16% tax
