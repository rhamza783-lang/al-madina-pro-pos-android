# Required for AndroidX
android.useAndroidX=true

# Suppress compileSdk 34 warning
android.suppressUnsupportedCompileSdk=34

# Suppress AAR metadata validation (fixes checkDebugAarMetadata failure)
android.suppressAarMetadataValidation=true

# Optional: Enable full warnings
org.gradle.warning.mode=all
